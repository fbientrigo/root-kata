# STATE — ROOT WebAssembly Subset

## Current gate

**M6a — the shipped curriculum runs in the browser: PASS, independently reviewed.**
11 of the 13 katas are completed in Chromium with output byte-identical to
native ROOT and graded passed by their own validators. The remaining two are
blocked on `TClass` reflection (M4b).

Run everything with `bash wasm/run-gates.sh`. Open it yourself with
`bash wasm/gates/rootweb/run.sh serve`.

| milestone | gate | state |
| --- | --- | --- |
| M1 ROOT-owned Hist closure built for wasm | `gates/p0deps/xbuild` | **PASS**, independently reproduced |
| M2 those libraries loading and running under Node | `gates/rootlight` | **PASS**, independently reproduced |
| M2b the same in Chromium, plus an interactive page | [`gates/rootweb`](gates/rootweb/FINDINGS.md) | **PASS**, independently reproduced |
| M3 ROOT's TInterpreter over CppInterOp; P0 parity | [`gates/rootinterp`](gates/rootinterp/FINDINGS.md) | **PASS**, independently reproduced |
| M4a TF1/TFormula/TGraph parity | [`gates/rootformula`](gates/rootformula/FINDINGS.md) | **PASS**, independently reproduced |
| M6a the shipped ROOT Kata curriculum, in the browser | [`gates/rootkatas`](gates/rootkatas/FINDINGS.md) | **PASS**, independently reviewed |

Genuine CERN ROOT 6.40.04 now runs in Chromium over plain static HTTP with no
COOP/COEP, and matches native ROOT byte for byte on both probes: 39 TH1D/TAxis
values (P0) and 21 TF1/TFormula/TGraph values (P1), all at `%.17g`.

The interpreter now has **284** loud-failure overrides against 18 implemented
names, covering ROOT's non-pure `TInterpreter` defaults as well as
its pure virtuals — the gap the M3/M4 review recorded as the most important
unproven claim. Re-running every gate against the stricter adapter changed
nothing, so no proven path was relying on a silent default.

Not established: `TH1::Fit` and `cpp-root-histogram` (both blocked on `TClass`
reflection — measured, not assumed: each stops at `TInterpreter::SetClassInfo`,
so M4b unblocks both), `TFile`, `TTree`, graphics, in-browser grading (the
validators still run in CPython on the developer's machine), and the product
runner at `docs/site.js:238`, which is untouched.

## Confirmed facts

1. G0 remains reproducible with pinned Emscripten **4.0.9**, ROOT source **6.40.04**, Node and headless Chromium.
2. `PtEtaPhiMVector` is a genuine ROOT alias at `math/genvector/inc/Math/Vector4Dfwd.h:84`. Its common template path through `Math/Vector4D.h` compiles without linking a ROOT library.
3. G2 generates `RConfigure.h` at run time from pinned ROOT `config/RConfigure.in`, using CMake `configure_file`, the mechanism ROOT uses in `cmake/modules/RootConfiguration.cmake:549-573`. It probes the actual native/`em++` compiler for C++ standard and attribute support; its no-component profile leaves optional ROOT component macros undefined. No hand-written or checked-in compatibility header exists.
4. `bash wasm/gates/g2/run.sh` builds a native reference, Node wasm target, and browser wasm target from the same unmodified ROOT headers and program. All emitted:

   ```
   pt=50.000000 eta=1.200000 phi=0.500000 m=0.105658
   E=90.532840 px=43.879128 py=23.971277 pz=75.473068
   sum_m=54.847433 sum_pt=52.238727
   ```

5. G2 deliberately failed (non-zero, unified diff) after corrupting its expected `pt` to `999.000000`; the fixture was restored.
6. The normal library route remains expensive: Core has unconditional `add_dependencies(Core CLING rconfigure)` (`core/CMakeLists.txt:42`), while MathCore and Hist depend on Core.
7. [ROOT_LIGHT.md](ROOT_LIGHT.md) records the evidence-backed 20/80 inventory. The only Tier 1 computation is the restricted GenVector slice. Histograms are the strongest actual curriculum demand but remain Tier 2; TFormula/fitting, TFile/TTree and RDataFrame are Tier 3 under current evidence.
8. G4 ran the pinned `em++` direct-link probe with genuine `TH1D`, no ROOT libraries, and the G2-generated `RConfigure.h`. Its first missing symbol was `TH1D::TH1D(char const*, char const*, int, double, double)`. The reproducible error is preserved in `wasm/build/g4/first-missing-symbol.txt`. **Corrected rationale** (superseding an earlier, weaker version of this note): the blocker is not merely that Hist's CMake target depends on MathCore/Matrix/RIO which depend on Core which depends on CLING — `add_dependencies(Core CLING rconfigure)` (`core/CMakeLists.txt:42`) is a CMake *build-order* edge, not a link edge, and Core's actual `target_link_libraries` (`:44-49`) is only dl/threads/atomics; Cling is `dlsym`'d at runtime (`core/base/src/TROOT.cxx:2251`), not statically linked. The real blocker is `ClassDefOverride` (`hist/hist/inc/TH1.h:693,949`): it expands via `_ClassDefOutline_` (`core/base/inc/Rtypes.h:310-321`) to *declare without defining* `Class()`, `Streamer()`, `fgIsA`, `Dictionary()`; `IsA()`/`Streamer()` are virtual, so constructing one `TH1D` emits a vtable referencing those undefined symbols. Only `rootcling` supplies them (`core/dictgen/src/rootcling_impl.cxx:1122-1159`); `ClassImp` is a no-op in 6.40 (`Rtypes.h:373`), so there is no non-dictionary fallback. `TH1.cxx` is also a single 10,731-line TU pulling `TF1`/`TFormula`/`ROOT::Fit`/`GoFTest`, so the arithmetic-only `Fill`/`Get*` path cannot be linked in isolation either. See [g4/BLOCKER.md](gates/g4/BLOCKER.md) (corrected).
9. **Curriculum ROOT-API inventory** (all 13 exercises read directly): the shipped surface is `TH1D`/`TH1` (5 of 8 ROOT katas: ctor, `Fill`, `GetEntries`, `GetBinContent`, `SetBinContent`, `SetBinError`, `GetMean`, `GetStdDev`, `Integral`, `GetNbinsX`, `GetXaxis`, `Fit`, `Draw`), `TAxis` (4), `TF1`-from-formula-string (3, which transitively requires `TFormula`→`TInterpreter` — this is a **shipped**, not merely planned, interpreter dependency), `TGraph` data access (1), and one `TCanvas`/`gROOT::SetBatch`/`SaveAs("preview.png")` (1, decorative — no validator inspects the PNG). `TMath`, `TRandom`/`gRandom`, and `ROOT::Math` four-vectors (the G2 slice) appear **nowhere** in the curriculum, planned or shipped. Planned m2–m6 milestones add `TFile`/`TTree`, `RDataFrame`, `RVec`. Source: `curriculum/triads/*.csv`, `curriculum/plan.json`, `src/root_kata/exercises/*/harness.cpp`.
10. **The browser runner today does no client-side compilation of any kind.** `docs/site.js` (`fetch('/api/run')`) posts student code to a *local* Python server (`src/root_kata/web_server.py`, bound to `127.0.0.1`) which shells out to real `g++`/`root-config` — this is the existing, honest "no Cling, no PyROOT" design (`src/root_kata/cpp_runner.py:1-21`), not a mock of ROOT. There is no WASM, xeus-cpp, or JSROOT integration anywhere in `docs/`/`src/`/`scripts/`. `docs/site.js:231-254`'s single `fetch` call is the integration seam a wasm runner would replace. No COOP/COEP headers are set anywhere and GitHub Pages cannot set them, so any in-browser toolchain must be single-threaded (no `SharedArrayBuffer`, no pthreads).
11. **G7 PASS: a C++ compiler runs entirely client-side in Chromium and compiles genuine, unmodified ROOT headers correctly.** Two independent hypotheses ran in parallel against the identical G2 payload. **H1 (xeus-cpp-lite/CppInterOp, Clang-Repl-based) passed**: prebuilt `emscripten-forge-4x` conda packages (`xeus-cpp` 0.10.0, `cppinterop` 1.9.0, `xeus` 6.0.5), sha256-pinned and independently re-verified by the orchestrator against the channel's live `repodata.json`. The page drives a genuine `xkernel` via the real Jupyter `execute_request` wire protocol, mounts 355 real ROOT header files into its virtual FS, and produces output byte-identical to `wasm/gates/g2/expected.txt` — independently reproduced by the orchestrator twice consecutively from clean. A deliberately-broken variant produces a genuine Clang diagnostic (`error: no member named 'printfXX'... did you mean 'printf'?`) and no stdout. No `SharedArrayBuffer`/threads required — verified running under a plain `python3 -m http.server` with no COOP/COEP headers, satisfying the GitHub Pages hosting constraint. Payload: ~99.5 MiB. **H2 (Clang+LLD as WebAssembly, AOT/WASI-targeted) was falsified**: the prebuilt `browsercc` npm package genuinely compiles the same payload against the same real ROOT headers, but linking fails on `__cxa_allocate_exception`/`__cxa_throw` — every WASI-lineage prebuilt clang+lld shares a `libc++abi` with no exception-throwing runtime (a known, open upstream wasi-sdk gap), and ROOT's `GenVector_exception.h` deliberately keeps its `Throw()` inline so interactive `PtEtaPhiMVector` usage needs exceptions. See `gates/g7/FINDINGS.md` (synthesis) and `gates/g7/H2-FINDINGS.md` (H2 detail).
12. **G7 proves the compiler exists; the G4/G5 interpreter probe now tests the open `TH1D` question.** G7's payload was deliberately the already-proven GenVector program so its pass/fail was unambiguously about the compiler, not ROOT. The pinned interpreter probe mounts 631 genuine ROOT headers and passes the enlarged GenVector control, but `#include <TH1D.h>` fails with `TVersionCheck::TVersionCheck(int)` while loading the first incremental module.
13. **G4 cross-build review: PARTIAL.** Independent reproduction with ROOT 6.34.10's unmodified upstream `hist/hist/inc/LinkDef.h` and native host `rootcling` generated genuine `TH1D::Class`, `Dictionary`, and `Streamer` code. Emscripten compiled that dictionary and genuine upstream `hist/hist/src/TH1.cxx` as WebAssembly objects, but the focused link fails on `TVersionCheck`, `TObject`, `TNamed`, `TString`, `TAtt*`, `TAxis`, and `TArrayD` symbols because the target-side `Hist + MathCore + Matrix + RIO + Thread + Core` closure was not built. The all-Emscripten top-level `Hist` attempt remains in embedded LLVM/Cling compilation under the bounded build, so no linked module, six-call execution, native comparison, Chromium run, or final payload size exists; see [gates/g4/rootcling-crossbuild/CODEX-REVIEW.md](gates/g4/rootcling-crossbuild/CODEX-REVIEW.md). This evidence uses a worker checkout at 6.34.10 rather than the G0-pinned 6.40.04 source, so pinned-release confirmation remains open.
14. **G4/G5 interpreter probe: BLOCKED-AT-SYMBOL.** `bash gates/g4/interpreter-probe/probe.sh` independently reproduced the pinned ROOT 6.40.04 result: the identical enlarged 631-header mount passes G2's GenVector control, while `#include <TH1D.h>` fails with `Dynamic linking error: cannot resolve symbol _ZN13TVersionCheckC1Ei`. The pinned source confirms `TVersionCheck.h:31` creates the file-scope static, `TObject.h:18` includes it, and `TSystem.cxx:4462` defines its constructor. This corroborates the prior Core closure finding and closes the cheap interpreter path; no G5 call executes.

15. **P0 native audit: conditional interpreter dependency.** Independently reproduced on 6.40.02 and 6.34.10: stock 6.40 TH1D initialization reaches libCling through the autoregistration configuration lookup; denial aborts. Diagnostic E1 interposition preserves default P0 output without libCling, while the denied interpreter control still fails. This proves a narrow semantic isolation, not a configuration-preserving port; 6.40.04 runtime parity remains untested.
16. **ROOT-target Core build: PASS, runtime PARTIAL.** Independent fresh-cache reproduction with pinned 6.40.04/Emscripten 4.0.9 and version-mismatched host rootcling 6.40.02 completes ROOT's own Core target. The eight-file platform patch supplies the target generator environment, identifies wasm32 in RConfig/TUnixSystem, forwards builtin toolchains and schedules ZSTD. Core's 14-node order closure contains no target LLVM/Cling build nodes. Selected target class layouts agree with em++; the generator overlay does not reach target compilation. All members of the five codec/regex archives are wasm and their implementations are included, but no codec round trip or runtime reflection is tested. `libCore.so` remains relocatable, `-pthread` remains, and `InitInterpreter` is unchanged. Non-empty PCM serialization crashes, but pinned upstream MathCore, Matrix, Hist, RIO and Thread explicitly request empty PCM; that crash is not an established blocker for their stock targets. Candidate packages remain Core, Thread, RIO, MathCore, Matrix and Hist with `imt=OFF`. See [Core-fix review](gates/p0deps/CODEX-REVIEW-CORE.md), superseding the earlier [P0 compile blocker](gates/p0deps/CODEX-REVIEW.md).

17. **S0 census (2026-09-17): measured, not assumed — exactly one class reaches `SetClassInfo` before a new wall.** Method: `wasm/rootlight/interpreter/TCppInterOpInterpreter.{h,cxx}` got a throwaway `SetClassInfo(TClass *cl, Bool_t, Bool_t)` override (header declaration + `implemented.txt` entry, so `gen_fatal.py` stops generating a loud override for it — clean add, exactly 1 overload per `TInterpreter.h:213`) that prints `cl->GetName()`/`cl->GetState()` via `::Info` and returns without touching `cl->fClassInfo` (private, `TClass`-only-friends-`TCling`; a ROOT-legal "no info available" per `TClass::Init`, `$R/core/meta/src/TClass.cxx:1486-1487`). `CheckClassInfo` (`TCppInterOpInterpreter.cxx:424-436`) got a matching `::Info` print on the miss branch (`Cpp::GetScopeFromCompleteName(name)` null). Rebuilt with `bash wasm/rootlight/interpreter/build.sh` (283 loud overrides, 19 hand-implemented; clean link, no FATAL from the overload-count check) and ran `bash wasm/gates/rootformula/run.sh loud`, capturing `$OUT/fit.diag` (`~/.cache/rootwasm-p0/rootformula/fit.diag`) for the M4a `fit.cxx` fixture (`wasm/gates/rootformula/run.sh:36-51`: `TH1D h(20,-5,5)` + `TF1 f("f","gaus",...)` + `h.Fit(&f,"QN0")`).
    - **Ordered `SetClassInfo` census: `TObject` (state=4, i.e. `kHasTClassInit`/`kLoaded` per `TClass.h:130-134`) — exactly once.** No second class reached `SetClassInfo` in this run; the earlier working assumption of "a single blocking class" undercounted only by coincidence — the real reason is the next wall (below) stops the whole path before any other class's `TClass::Init` gets there.
    - **`CheckClassInfo` misses: `TObjString`** — this name never resolves to a clang scope in the kernel's clang at all (`Cpp::GetScopeFromCompleteName` returns null), i.e. it is Family-0 evidence: `TObjString` would need an `#include` to become visible to this interpreter, independent of any `SetClassInfo` question.
    - **The next wall, verbatim from `fit.diag`:** `Error in <TCppInterOpInterpreter>: TInterpreter::IsAutoParsingSuspended is not implemented in wasm ROOT yet`, immediately followed by `Could not load dynamic lib: /tmp/incr_module_3.wasm` / `[object WebAssembly.Exception]` / `Failed to execute via ::process:Failed to load incremental module`. Pinned-source cross-check: `IsAutoParsingSuspended` (`$R/core/meta/inc/TInterpreter.h:118`) is read by `TClass::GetClass` (`$R/core/meta/src/TClass.cxx:3077`) through the `TInterpreter::SuspendAutoParsing` RAII fence (`TInterpreter.h:111-116`) — i.e. the wall is one level up the call stack from `SetClassInfo`, in the class-lookup path that would ask for the *next* class by name (plausibly `TObjString`, given the miss above), not inside `TClass::Init` itself.
    - This is a measurement only: no `implemented.txt` semantics for `IsAutoParsingSuspended` were added, no `Family A/B/C` reflection was implemented, `$R` was not modified, and all instrumentation above remains an uncommitted, throwaway diagnostic in the working tree pending the next step's decision to build on it or discard it.

18. **`SetSuspendAutoParsing`/`IsAutoParsingSuspended` implemented for real (2026-09-17), unlike fact 17's throwaway `SetClassInfo`/`CheckClassInfo` prints — this one is genuine, truthful state, not measurement.** Continuing directly from fact 17's new wall: `TCppInterOpInterpreter` (`wasm/rootlight/interpreter/TCppInterOpInterpreter.h`) got `bool fAutoParsingSuspended = false;` plus `Bool_t SetSuspendAutoParsing(Bool_t value) override` (sets the flag, returns the value it held *before* the call) and `Bool_t IsAutoParsingSuspended() const override` (returns the current flag). This is answerable honestly with a plain bool because `TInterpreter::SuspendAutoParsing` (`$R/core/meta/inc/TInterpreter.h:111-116`) is a pure RAII fence over exactly this pair — `SetSuspendAutoParsing` (`:64`, declared `protected`, friended to `SuspendAutoParsing`) sets and returns the previous value, `IsAutoParsingSuspended` (`:118`) reads it back — and `TClass::GetClass` (`$R/core/meta/src/TClass.cxx:3076-3083`) is the caller, using it only to decide whether to hold off on auto-parsing, not to perform any reflection itself. Both names have exactly 1 overload in `TInterpreter.h` (`:64` and `:118`), matching `gen_fatal.py`'s overload-coverage check with no FATAL. Added to `implemented.txt` under a new "genuinely truthful state" section (distinct from fact 17's "measurement only" section, so the distinction stays visible in the file itself). Rebuilt clean: `bash wasm/rootlight/interpreter/build.sh` now reports 281 loud-failure overrides, 21 hand-implemented (up from fact 17's 283/19 — two names added, and the header additions also removed two now-redundant generated overrides for the same names).
    - Re-ran `bash wasm/gates/rootformula/run.sh loud`: the `IsAutoParsingSuspended` wall is gone; the fixture advances past it and hits a **new wall**, verbatim from `~/.cache/rootwasm-p0/rootformula/fit.diag`:
      ```
      Info in <TCppInterOpInterpreter::CheckClassInfo>: S0 miss: TObjString
      Info in <TCppInterOpInterpreter::SetClassInfo>: S0 census: TObject (state=4)
      Error in <TCppInterOpInterpreter>: TInterpreter::AutoParse is not implemented in wasm ROOT yet
      Could not load dynamic lib: /tmp/incr_module_3.wasm
      [object WebAssembly.Exception]
      Failed to execute via ::process:Failed to load incremental module
      ```
      The `SetClassInfo`/`CheckClassInfo` census lines are identical to fact 17's (still exactly one `SetClassInfo` call, `TObject`; still one `CheckClassInfo` miss, `TObjString`), confirming `IsAutoParsingSuspended` really was the very next and only thing gating progress past that point.
    - **Stopping here, not implementing `AutoParse`.** `AutoParse` (`$R/core/meta/inc/TInterpreter.h:144`, called out explicitly in this task's own briefing as "separate, bigger family — NOT in scope here") is not a state query: `TClass::GetClass`'s use of it (immediately after the `SuspendAutoParsing` fence this fact closes) exists specifically to parse a class's header on demand so the interpreter's ClassInfo/scope becomes available — i.e. it is the mechanism that would make `CheckClassInfo`'s `TObjString` miss (fact 17) resolve. Implementing it truthfully means real header/module loading feeding into ClassInfo resolution, squarely in the ClassInfo/MethodInfo/DataMemberInfo/CallFunc reflection family this task was scoped to avoid, and far past a ~10-line orthogonal flag. Per this task's instructions, reporting back rather than proceeding.

19. **M4b Family 0: `AutoParse` implemented for real (2026-09-17), closing the wall fact 18 stopped at.** `TCppInterOpInterpreter::RegisterModule` (`wasm/rootlight/interpreter/TCppInterOpInterpreter.cxx`) now parses the `classesHeaders` table into a genuine `std::unordered_map<std::string, std::vector<std::string>> fClassesHeadersMap`, following the exact grouping `TCling::RegisterModule` uses (`$R/core/metacling/src/TCling.cxx:2275-2313`: one class name, then its header list, terminated by an `"@"` marker, repeated across the array) — a plain string key stands in for TCling's hash-of-name bookkeeping, which exists only to dedupe hash collisions this adapter has no need to reproduce. `AutoParse(const char *cls)` looks the class up in that map; for each header not already in a new `std::set<std::string> fAutoParsedHeaders`, it calls `Cpp::Declare(("#include <" + header + ">").c_str())`, throws a named `std::runtime_error` (mirroring `rkUnsupported`'s two-channel loud-failure convention: `::Error` plus an exception) if the `#include` fails to compile, and returns `1`/`0` exactly matching `TCling::AutoParse`'s own documented contract ("Returns 1 in case of success, 0 in case of failure", `$R/core/metacling/src/TCling.cxx:6641-6643`) rather than the raw header count. Added to `implemented.txt`; `TInterpreter.h:144` has exactly 1 overload, so `gen_fatal.py`'s overload-coverage check passes with no FATAL. Rebuilt clean: 280 loud-failure overrides, 22 implemented by hand.
    - **Verification: real progress, but a new failure mode, not success.** Re-running `bash wasm/gates/rootformula/run.sh loud`'s `fit.cxx` fixture (`TH1D`+`TF1("gaus")`+`h.Fit(&f,"QN0")`) no longer throws at all: `fit.diag` came back completely empty and `fit.out` printed `fit.start`/`fit.returned`. A follow-up probe (`p0=%g p1=%g p2=%g chi2=%g ndf=%d` after `h.Fit(&f,"QN0S")`) showed `chi2=0 ndf=0` — the classic signature of a minimizer that was never actually run, not a converged fit. This is the exact "silent no-op" the project's rules forbid, and it is expected at this exact point: `AutoParse` alone lets `TClass::GetClass`/`TClass::LoadClassInfo` (`$R/core/meta/src/TClass.cxx:5934-5945`) get past the `SuspendAutoParsing` fence and the header-on-demand step without exception, but `SetClassInfo` was still fact 17's throwaway (never sets `cl->fClassInfo`), so `ROOT::Math::Factory`'s plugin-manager construction of `Minuit2Minimizer` fails to get a usable `TClass` and the fit silently does nothing while still reporting success. This is exactly why Family A (fact 20) has to follow immediately, not a separate, later step.

20. **M4b Family A: real `SetClassInfo`, a 9th patched file, and a verified `DeclId_t`/`ClassInfo_t` round trip (2026-09-17).**
    - **The `TClass.h` patch and how it's tracked.** `TClass::fClassInfo` is private, writable only by `friend class TCling;` (`$R/core/meta/inc/TClass.h:86`, pre-patch). This project already tracks its ROOT-source platform patch as a single git-diff-style file, `wasm/gates/p0deps/xbuild/host-rootcling.patch` (applied with `patch -p1` against the copy `wasm/gates/p0deps/xbuild/run.sh` makes at `$X/src` before configuring the target-side Core/Hist build — see that script's `configure` step) — this is the actual "eight-file platform patch" fact 16 refers to (it has grown to 13 files as the project progressed; the name stuck). Following that exact convention, a 9th hunk was appended to the end of the same patch file, adding `friend class TCppInterOpInterpreter;` right after `friend class TCling;` (context line renumbered to `@@ -84,6 +84,10 @@` to match the pristine tarball's actual line numbers, verified against a fresh extraction of the pinned `root_v6.40.04.source.tar.gz`). Because a `friend` declaration has zero ABI/layout impact, no library needed rebuilding: the same one-line addition was also applied directly (not just tracked) to `$R/core/meta/inc/TClass.h` itself and to the three downstream copies this milestone's build chain already had cached — `$X/src` (xbuild's own patched copy), `$X/build/include` (its CMake build-tree header copy), and `~/.cache/rootwasm-p0/rootlight/rootsys/include` (the staged ROOTSYS the interpreter's `build.sh` actually compiles against) — so the fix took effect immediately without a ~7-minute Core/Hist rebuild. All four copies were confirmed identical at the patched line before and after.
    - **`SetClassInfo` is now genuine**, mirroring `TCling::SetClassInfo` (`$R/core/metacling/src/TCling.cxx:4154-4269`) narrowed by two omissions specific to this adapter: no `fIsShuttingDown` branch (this adapter has no such state) and no `"tuple<...>"` `AlternateTuple` rewrite (`TCling.cxx:4196-4207`, ROOT's I/O-friendly tuple emulation, unrelated to reflection identity). It handles the reload path (removes the old `DeclId_t` mapping via `TClass::RemoveClassDeclId`, deletes the old handle), the already-set/no-reload no-op, the "no info available" fallback (`TCling.cxx:4186-4195`'s `SetWithoutClassInfoState`, chosen by whether `cl->fStreamerInfo` has entries), and on success assigns `cl->fClassInfo` for real (legal now via the friend patch), sets `cl->fState` to `kInterpreted` unless a real dictionary already applies, and calls `TClass::AddClassToDeclIdMap`. Scope lookup uses `Cpp::GetScopeFromCompleteName(cl->GetName())`, not bare `Cpp::GetScope`: empirically confirmed `GetScope`/`GetNamed` do not split `"::"` themselves (`CppInterOp.cpp`'s `GetNamed` hands the whole string to `cling::utils::Lookup::Named`, which looks up one `DeclarationName` — verified by reading `cling/lib/Utils/AST.cpp:1468-1471`, which builds a single `Idents.get(Name)` from the whole string), so a qualified name like `ROOT::Minuit2::Minuit2Minimizer` would never resolve through it; `GetScopeFromCompleteName` (which does split on `"::"`) is the pre-existing convention this file already used everywhere else a qualified name is resolved (`CheckClassInfo`, `ClassInfo_Factory(const char*)`, `ClassInfo_Init`), so `SetClassInfo` keeps that one convention rather than introducing a second.
    - **Tagged handles.** `RkClassInfo` and `RkCallFunc` (the anonymous-namespace structs behind every `ClassInfo_t*`/`CallFunc_t*` this adapter hands ROOT) both gained a first `RkHandleTag fTag` member (`kClassInfo`/`kCallFunc`, two `uint32_t` constants). The `CI()`/`CF()` accessors now check the tag and call `rkUnsupported` by name on a mismatch instead of silently reinterpreting the wrong struct layout — one enum, two one-line checks, no new framework.
    - **`ClassInfo_Factory(DeclId_t)` and `GetDeclId(ClassInfo_t*)` implemented as a verified round trip.** `TDictionary::DeclId_t` is `const void*` (`$R/core/meta/inc/TDictionary.h:224`) and CppInterOp's `TCppScope_t` is `void*` (`CppInterOp.h:38`) — the same canonical clang `Decl*` in this adapter, so both are plain casts: `ClassInfo_Factory(DeclId_t)` wraps the pointer in a tagged `RkClassInfo`, `GetDeclId(ClassInfo_t*)` unwraps it back. Verified empirically, not just argued: a probe called `gInterpreter->ClassInfo_Factory("TObject")`, then `GetDeclId`, then `ClassInfo_Factory` again on that id, then `GetDeclId` once more — both `ClassInfo_t` handles reported `ClassInfo_IsValid() == true` and both `DeclId_t` values were bit-identical (`declid=0x3477710` both times). `GetDeclId` has 6 overloads in `TInterpreter.h` (`CallFunc_t`, `ClassInfo_t`, `DataMemberInfo_t`, `FuncTempInfo_t`, `MethodInfo_t`, `TypedefInfo_t`); only `ClassInfo_t` is genuinely implemented, the other 5 are declared by hand in the header as explicit `rkUnsupported` overrides (required by `gen_fatal.py`'s overload-coverage check once any overload of a name is in `implemented.txt`), keeping the Method/DataMember/CallFunc reflection families this task is scoped to avoid firmly unimplemented rather than silently inherited. Rebuilt clean: 274 loud-failure overrides, 23 implemented by hand.
    - **The new wall (verbatim), found two ways.** `bash wasm/gates/rootformula/run.sh loud` now **passes** (the milestone boundary moved, so the gate's own assertion — "stops loudly, names the missing service" — is satisfied by a different, later service than before) and reports `TH1::Fit stops loudly at: TInterpreter::ClassInfo_Title`. `fit.diag` verbatim:
      ```
      Info in <TCppInterOpInterpreter::CheckClassInfo>: S0 miss: TObjString
      Error in <TCppInterOpInterpreter>: TInterpreter::ClassInfo_Title is not implemented in wasm ROOT yet
      Could not load dynamic lib: /tmp/incr_module_3.wasm
      [object WebAssembly.Exception]
      Failed to execute via ::process:Failed to load incremental module
      ```
      A second, independent probe (`TClass::GetClass("TObject")->GetClassInfo()`) hit the identical wall for a class with a real compiled dictionary, not just an interpreted one — confirming this is universal, not `TObjString`-specific. Source: `$R/core/meta/src/TClass.cxx:1185` and `:1583`, both `if (fClassInfo) SetTitle(gCling->ClassInfo_Title(fClassInfo));`, called from `TClass::Init`/`TClass::Property()` immediately after any successful `SetClassInfo`. This is earlier and narrower than the anticipated `TClass::GetMethodWithPrototype` wall (still a `ClassInfo_` property query, not Method/DataMember/CallFunc overload resolution), but implementing it was not attempted: it is outside this task's Part 1/Part 2 checklist, and per this task's instructions, the boundary is reported rather than pushed further.
    - Nothing was committed; `$R` was edited in place (as instructed) with the same one-line change tracked in `host-rootcling.patch` for reproducibility from a clean fetch.

21. **M4b: `ClassInfo_Title` implemented for real (2026-09-17), and the wall it uncovers is genuinely out of scope — the milestone stops here.**
    - **`ClassInfo_Title(ClassInfo_t *info)`** (`wasm/rootlight/interpreter/TCppInterOpInterpreter.cxx`, declared `override` in `TCppInterOpInterpreter.h`) unwraps the tagged handle with the existing `CI()` accessor and returns `""` for an invalid/empty scope, else `Cpp::GetDoxygenComment(h->fScope)` (default `strip_comment_markers = true`) stored into a new `std::string fTitle` member of `RkClassInfo` before returning `.c_str()` — mirroring `TClingClassInfo`'s own lifetime convention: real ROOT's `TClingClassInfo::Title()` (`$R/core/metacling/src/TClingClassInfo.cxx:1446-1483`) stores into its own `fTitle` member for the same reason (the pointer must outlive the call), so this is the first `const char*`-returning method in this adapter and it sets that convention deliberately rather than guessing at one. **Why `GetDoxygenComment` and not a flat `""`:** the task's own briefing anticipated CppInterOp having no comment API and pre-approved a flat empty string in that case; a re-grep of the pinned CppInterOp 1.9 header (`~/.root-kata-wasm/xcpp-toolchain/cppinterop-extract/include/CppInterOp/CppInterOp.h:457-464`) found one was missed: `CPPINTEROP_API std::string GetDoxygenComment(TCppScope_t scope, bool strip_comment_markers = true)`, confirmed actually linkable (not just declared) by `strings`-grepping the wasm-built `libclangCppInterOp.so` the kernel loads — `_ZN7CppImpl17GetDoxygenCommentEPvb` is an exported symbol. It is a genuinely narrower answer than real ROOT's (`TClingClassInfo::Title` checks a clang `annotate` attribute — rootcling's encoding of a trailing `//comment` — then `ROOT::TMetaUtils::GetClassComment`, i.e. exactly that trailing-comment convention; `GetDoxygenComment` instead reads clang's standard preceding `///`/`/** */` doc-comment attachment, a different comment syntax/position). It is not fabrication either way: a class with no doc comment truthfully gets `""`, and a class that does have one gets its real text, not an invented one. Added to `implemented.txt`; `TInterpreter.h:438` has exactly 1 `ClassInfo_Title` overload, confirmed both by direct `grep` and by `gen_fatal.py` completing with no FATAL overload-count mismatch. Rebuilt clean: `gen_fatal: 273 loud-failure overrides, 24 implemented by hand` (down from fact 20's 274/23).
    - **The gate did not hit another wall — it hit the actual milestone boundary.** `bash wasm/gates/rootformula/run.sh loud` no longer fails loudly at all: it fails with `rootformula M4a FAIL: TH1::Fit returned a result, but TClass reflection is not implemented -- a fit nothing computed is exactly the silent no-op this project forbids` — the gate script's own pre-written assertion for exactly this outcome (`wasm/gates/rootformula/run.sh:58-60`), because `fit.out` contains `fit.returned` and `fit.diag` is completely empty (verified directly: `~/.cache/rootwasm-p0/rootformula/fit.diag` is 0 bytes, `fit.out` is only `fit.start`/`fit.returned`, no `MATH_ERROR_MSG`/`::Error` from anywhere). `ClassInfo_Title` was the last `TClass::Init`-path blocker (fact 20's wall, `TClass.cxx:1185`/`:1583`), so `TH1D`/`TF1`'s `TClass` objects now fully initialize and `TH1::Fit` reaches `ROOT::Fit::Fitter::DoInitMinimizer` (`$R/math/mathcore/src/Fitter.cxx:697-700`), which calls `fConfig.CreateMinimizer()` → `ROOT::Math::Factory::CreateMinimizer` (`$R/math/mathcore/src/Factory.cxx:63-108`) → `gROOT->GetPluginManager()->FindHandler("ROOT::Math::Minimizer", "Minuit")` → (if found) `TPluginHandler::ExecPlugin` → `TPluginHandler::SetupCallEnv` (`$R/core/base/src/TPluginManager.cxx:196-247`): `TClass::GetClass(fClass)` by name, then **`cl->GetMethodWithPrototype(method, proto)`** (real method-name-plus-argument-types overload resolution, not this adapter's name-only `CallFunc_SetFuncProto`), then `TMethodCall::Init`/execution to actually **construct** the concrete `Minimizer` subclass. That is squarely the method-enumeration/overload-resolution/object-construction boundary this task's role rules say to stop at (`GetMethodWithPrototype`, and the `CallFunc_*`-execution/construction family) — whether the immediate proximate cause is a missing plugin registration (`CreateMinimizer` returns `nullptr` silently with **no** diagnostic call on that specific branch, `Factory.cxx:106-107`, which would itself explain the empty `fit.diag`) or a present one, the very next genuine step either way is `GetMethodWithPrototype` + construction. **Stopping here per this task's explicit instructions; not implemented.**
    - Nothing committed. `wasm/rootlight/interpreter/TCppInterOpInterpreter.{h,cxx}` and `wasm/rootlight/interpreter/implemented.txt` are the only files touched this session, plus this `STATE.md` entry.

22. **M4b Families B-F implemented (2026-09-17); the crux (`MethodArgInfo_TypeNormalizedName` vs `TClassEdit::GetNormalizedName`) genuinely mismatches -- stop condition #1, hit with byte-for-byte evidence on both sides, plus two more independent walls one level up in the same chain. Not committed.**
    - **What got implemented, and verified by direct source reads plus a live build.** `ClassInfo_Contains` (discovered mid-task, not in the original family list: `TListOfFunctions::Get`, `$R/core/meta/src/TListOfFunctions.cxx:320-324`, is what `TClass::GetMethodWithPrototype` actually goes through once it has a `DeclId_t`, and checks this before ever building a `TMethod` — genuine via `Cpp::GetParentScope`'s canonical-decl identity, the same identity model fact 20 already verified for `ClassInfo_t`/`DeclId_t`). **Family B**: `MethodInfo_Factory(ClassInfo_t*/DeclId_t)`, `_Next`, `_IsValid`, `_Delete`, `_FactoryCopy`, `_Name` over a tagged `RkMethodInfo` handle caching one `Cpp::GetClassMethods` enumeration (the single-enumeration-source rule), with the `-1`/`Next()`-first convention for the enumerating factory and an immediately-valid `fIdx=0` for the `DeclId_t` factory (matches `TListOfFunctions.cxx:330`'s no-`Next()`-before-use call site). **Family C**: `MethodInfo_Property`/`_ExtraProperty` (OR-ing `Cpp::IsPublicMethod`/`IsProtectedMethod`/`IsPrivateMethod`/`IsStaticMethod`/`IsVirtualMethod`/`IsConstMethod`/`IsExplicit`/`IsConstructor`/`IsDestructor` into ROOT's `EProperty`/`EFunctionProperty` bits, `$R/core/meta/inc/TDictionary.h:64-135` — narrower than `TClingMethodInfo::Property`/`ExtraProperty`, `TClingMethodInfo.cxx:436-548`, which additionally does raw clang-AST work this task's chain never needs), `MethodInfo_NArg`/`_NDefaultArg` (`Cpp::GetFunctionNumArgs`/`GetFunctionRequiredArgs`), and **`MethodInfo_Title`** — not in the original family list either, but required unconditionally by `TFunction`'s own constructor (`$R/core/meta/src/TFunction.cxx:43`), same `Cpp::GetDoxygenComment` convention as fact 21's `ClassInfo_Title`. **Family D**: `MethodArgInfo_Factory(MethodInfo_t*)`, `_FactoryCopy`, `_Delete`, `_Next`, `_IsValid`, `_Name`, `_TypeName`, `_TypeNormalizedName`, `_DefaultValue`, and `_Property` (genuine via `Cpp::HasTypeQualifier(type, Cpp::Const)`/`IsPointerType`/`IsReferenceType` — real type predicates, not string heuristics; not reached by this task's chain, kept for API completeness). **Family E**: `GetFunctionWithPrototype` and a real `CallFunc_SetFuncProto` replacing the old name-only stub, built on a shared `ResolveFunctionWithPrototype` helper: split `proto` on top-level commas (`SplitProtoTypes`, the exact depth-counter algorithm `gen_fatal.py:89-103`'s `strip_defaults` already uses, ported to C++), resolve each piece to a type, filter `Cpp::GetClassMethods(scope)` by `Cpp::GetName(f)==method`, `Cpp::BestOverloadFunctionMatch`, a paranoia membership check against the candidate set, and a `kExactMatch` post-filter comparing canonical type strings per argument. **Family F**: `CallFunc_SetFunc(CallFunc_t*, MethodInfo_t*)` and `CallFunc_ExecWithArgsAndReturn`, dispatching on `Cpp::IsConstructor`: a constructor goes through `Cpp::MakeFunctionCallable(func)`'s `JitCall::InvokeConstructor(&object, 1, {args,nargs}, /*is_arena=*/nullptr)` (verified against CppInterOp's own `Cpp::Construct`, `interpreter/CppInterOp/lib/CppInterOp/CppInterOp.cpp:4122-4152`, which uses the identical `void *result=arena; JC.InvokeConstructor(&result,count,args,is_arena)` pattern for the heap-allocating case), writing the resulting object pointer into `*(void**)ret` — traced precisely against `TMethodCall::Execute(void*,const void*[],int,void*)` (`TMethodCall.cxx:551-557`) and `TPluginHandler::ExecPluginImpl`'s `Longptr_t ret; fCallEnv->Execute(nullptr,args,nargs,&ret);` (`TPluginManager.h:184-193`), confirming `ret` is the address of a pointer-sized slot the wrapper fills, not a value ROOT pre-populates. A non-constructor keeps using the pre-existing `MakeRootCallable` wrapper-compile-and-call path. Rebuilt clean: `gen_fatal: 243 loud-failure overrides, 49 implemented by hand` (up from fact 21's 273/24 — 25 names added).
    - **Two primitive-composition fixes, found empirically, not assumed, needed to make Family E work at all (both under the ~30-40 line budget, both composing real CppInterOp primitives rather than hand-rolling a type grammar).** (1) `Cpp::GetType(piece)` — the primitive the task's own design named — only resolves a bare identifier or a fixed fundamental-keyword table (verified by reading `CppInterOp.cpp`'s `findBuiltinType`/`GetType`); it cannot parse `"Double_t const*"` or `"const char*"` at all. Routing the *pre-existing, already-passing* M4a `CallFunc_SetFuncProto` global-scope calls (e.g. `TFormula`'s own lookup of `"exp"`) through the new prototype-based resolution surfaced this immediately: `rootformula run.sh loud` regressed to `printf("%s is unknown.\n", ...)` spam (`TFormula.cxx:3487`) because `Cpp::GetType("Double_t const*")` returned null. Fixed with `ResolveProtoToken`, a bounded decoration-stripper (trailing `*`/`&`, a leading-or-trailing `const` token) that reconstitutes the compound type through `Cpp::AddTypeQualifier`/`GetPointerType`/`GetReferencedType` around a resolved base name — composing real primitives to undo a fixed, narrow, ROOT-own grammar (`TClass::GetMethodWithPrototype`'s own doc comment, `TClass.cxx:4511`: `"must be of the form: 'char*,int,double'"`), not reimplementing a general C++ type parser. (2) Separately, `Cpp::GetClassMethods` only enumerates `CXXMethodDecl`s (`GetClassDecls<CXXMethodDecl>`), so a free function at global scope (never a `CXXMethodDecl`) is invisible to it; the single-enumeration-source rule was scoped, per the task's own text, to *class*-scope lookups only, and `ResolveFunctionWithPrototype` now branches: `Cpp::GetFunctionsUsingName` for the global scope (exactly what the old stub already used), `Cpp::GetClassMethods`-filtered-by-name for a real class scope. Both fixes verified by rerunning `bash wasm/gates/rootformula/run.sh parity`, which still reports `p1: native CERN ROOT 6.40.04 == wasm ROOT in Chromium, 21 values` — no regression to the M4a numeric path.
    - **A third, narrower fix inside `ResolveProtoToken` itself, found the same way.** `Cpp::GetType("char")` returns `clang::ASTContext::SignedCharTy`, not the true (distinct, in C++) `char` type — read directly in `findBuiltinType` (`interpreter/CppInterOp/lib/CppInterOp/CppInterOp.cpp`: the bare-`"char"` branch always returns the signed variant, never `Context.CharTy`). This produced `"const signed char*"` instead of `"const char*"` for the exact `Minuit2Minimizer(const char*)` case, which `Cpp::BestOverloadFunctionMatch` then correctly rejected (`signed char*`/`char*` are incompatible pointer types, not a standard conversion) — empirically caught via a decoupled probe printing each resolved `Cpp::GetTypeAsString(argType)`, see below. Fixed with `ResolveBaseType`: for the one verified-wrong keyword (`"char"`), declare a throwaway `using __rk_proto_type_N = char;` via `Cpp::Declare` and read the real type back via `Cpp::GetScopeFromCompleteName`+`Cpp::GetTypeFromScope` — the same "ask the real compiler, don't guess" approach `MakeRootCallable` already uses elsewhere in this file, not a hand-written keyword-spelling correction table.
    - **The decoupled-probe methodology, and why it was necessary.** `TClass::GetClass("ROOT::Minuit2::Minuit2Minimizer")` independently hits a *different*, earlier, out-of-scope wall — `TInterpreter::CreateListOfBaseClasses is not implemented in wasm ROOT yet` (a `BaseClassInfo_*`-family need, squarely stop condition #3's territory, triggered by `Minuit2Minimizer`'s real inheritance from `ROOT::Math::Minimizer`) — before ever reaching `GetMethodWithPrototype`. Separately, `gROOT->GetPluginManager()->FindHandler("ROOT::Math::Minimizer","Minuit2")` returns `nullptr` in this environment even though the real registration macro is staged and correct (`~/.cache/rootwasm-p0/rootlight/rootsys/etc/plugins/ROOT@@Math@@Minimizer/P010_Minuit2Minimizer.C`: `gPluginMgr->AddHandler("ROOT::Math::Minimizer","Minuit2","ROOT::Minuit2::Minuit2Minimizer","Minuit2","Minuit2Minimizer(const char *)")`) — a `TPluginManager`/`TSystem` directory-scan gap in this browser environment, unrelated to `TClass` reflection and outside this task's scope. Both confirmed by dedicated one-off probe cells run through `bash wasm/gates/rootweb/run.sh cell <file>` (not committed; scratch files only). Because both block the *real* `TH1::Fit` call chain independently of how correct Families B-F are, Family E/F correctness and the `TypeNormalizedName` crux were verified instead by calling the `TInterpreter` primitives this adapter implements directly (`gInterpreter->ClassInfo_Factory`/`GetFunctionWithPrototype`/`MethodInfo_Factory`/`MethodArgInfo_Factory`/`CallFunc_SetFuncProto`/`CallFunc_ExecWithArgsAndReturn`), bypassing `TClass`/`TPluginManager` entirely — a legitimate exercise of this adapter's own implementation, not a fabricated result.
    - **The decoupled probe's full, in-browser result (after both fixes above), verbatim:** `ci.valid=1`; `decl(conv)=0x5aaf348` and `decl(exact)=0x5aaf348` (both match modes resolve the *same* decl — real overload resolution across the 3 real `Minuit2Minimizer` constructors: `Minuit2Minimizer(EMinimizerType=kMigrad)`, `Minuit2Minimizer(const char *type)`, and the implicit copy constructor, confirmed via `Cpp::GetFunctionSignature` in the same probe); `mi.valid=1 name=Minuit2Minimizer nargs=1`; `mi.property.public=1`; `mai.next=1 mai.valid=1 mai.name=type`; **`mai.typenormalized="const char *"`**; `cf.valid=1`; `ret=0x5b43580` (a real, non-null, freshly heap-constructed object); `minimizer.algo=Minuit2`. Families B through F are therefore independently confirmed genuinely working end-to-end, including constructing a real `Minuit2Minimizer` and reading a real field back off it, through this adapter's own `TInterpreter` surface.
    - **The crux, and the stop condition it hits.** A host-side probe (g++, native, linked directly against `$R/core/foundation/src/TClassEdit.cxx` + its own `core/foundation/inc`/`res` headers, no wasm, no ROOT libraries beyond that one file) computed the exact string `TPluginHandler::CheckNameMatch` (`$R/core/base/src/TPluginManager.cxx:179-191`) compares against: `abi::__cxa_demangle(typeid(const char*).name())` → `"char const*"`, then `TClassEdit::GetNormalizedName(norm, "char const*")` → **`"const char*"`** (no space before the star). The decoupled probe above shows this adapter's own `MethodArgInfo_TypeNormalizedName` (`Cpp::GetTypeAsString(Cpp::GetCanonicalType(argType))`) produces **`"const char *"`** (a space before the star) for the identical `const char*` parameter. `CheckNameMatch` is a plain `==` string comparison with no re-normalization of the right-hand side (`TPluginManager.cxx:191`: `return norm_name == arg->GetTypeNormalizedName();`) — `"const char*" != "const char *"`, so `ExactArgMatch` would return `false` in ROOT's real call chain even with every Family B-F primitive working exactly as designed, sending `TPluginHandler::ExecPluginImpl` down the `SetParams`/`CallFunc_SetArg` fallback path (`TPluginManager.h:194-207`) — a different, larger, explicitly out-of-scope family (stop condition #2, and by the task's own instruction, not something to hand-patch with a spacing correction, which would be exactly the forbidden hand-written translation table between clang's and ROOT's type-spelling conventions). **This is stop condition #1, hit with byte-for-byte evidence on both sides, not inferred.**
    - **Stopping here per this task's explicit instructions.** All three walls found this session (the `CreateListOfBaseClasses`/`BaseClassInfo_*` need, the `TPluginManager` plugin-registration gap, and the `TypeNormalizedName` spacing mismatch) are independent and each alone would block the real `TH1::Fit` → `Minuit2Minimizer` chain; none was pushed through. `bash wasm/gates/rootformula/run.sh loud` reports the unchanged fact-21 wall (`TH1::Fit returned a result, but TClass reflection is not implemented`, empty `fit.diag`) because the plugin-registration gap is encountered first on that exact path; `bash wasm/gates/rootformula/run.sh parity` still passes (21 values, no regression). The interpreter build is clean (`gen_fatal: 243 loud-failure overrides, 49 implemented by hand`). Nothing committed; `wasm/rootlight/interpreter/TCppInterOpInterpreter.{h,cxx}` and `wasm/rootlight/interpreter/implemented.txt` are the only files touched this session, plus this `STATE.md` entry. All debug-only `printf`s added during diagnosis were removed before this build.

23. **All three fact-22 walls closed or genuinely narrowed (2026-09-17); a fourth, deeper wall found and correctly stopped at. `rootformula run.sh loud`'s surfaced message is unchanged (same `fit.diag`-empty assertion), but the internal cause has moved. Not committed.**
    - **Forensic starting point.** The working tree at session start already had Fix 1 (`MethodArgInfo_TypeNormalizedName` routed through `TClassEdit::GetNormalizedName`, `TCppInterOpInterpreter.cxx:1296-1306`) and Fix 2's two code paths (`ExecuteMacro`, `TCppInterOpInterpreter.cxx:637-655`; and the `preRun`-callback staging fix in both `wasm/gates/rootweb/node-probe.mjs` and `wasm/gates/rootweb/page/kernel-worker.js`, moving the `$ROOTSYS` FS-mount/`Module.ENV` assignment before `dynamicLibraries`' own ctors run) fully written and building cleanly (`gen_fatal: 242/50`, matching fact 22's `243/49` plus `ExecuteMacro`). Fix 3 (`CreateListOfBaseClasses`/`BaseClassInfo_*`) had **not been started at all** — no trace in the diff, and `fatal_methods.inc` still generated a throwing override for it. `wasm/gates/rootlight/run.sh` and `wasm/gates/rootweb/run.sh`'s `Minuit2` library additions, and the `host-rootcling.patch` `TClass.h` friend hunk, were already-complete fact-21/22 staging, not new work. `bash wasm/rootlight/interpreter/build.sh` built clean at session start — the killed agent had left no broken intermediate state; "rebuilt and staged correctly" in its last message was accurate, it was killed only while re-verifying, not mid-edit.
    - **Fix 1: confirmed correct as found, no changes needed.** Re-verified by rebuilding and re-running the exact decoupled-primitive probe fact 22 used (`gInterpreter->MethodArgInfo_Factory`/`_TypeNormalizedName` on `Minuit2Minimizer(const char *)`'s argument): unchanged from what fact 22 already reported working for this half of the crux; the byte-for-byte mismatch fact 22 found was in the OLD code (`Cpp::GetTypeAsString(Cpp::GetCanonicalType(...))` alone), which Fix 1 already replaced before this session began.
    - **Fix 2: genuinely done, but the code alone was not sufficient — one more empirically-discovered primitive was required. Verified end-to-end.** `gROOT->GetPluginManager()->FindHandler("ROOT::Math::Minimizer","Minuit2")` returned `nullptr` even with `ExecuteMacro` and the staging fix both correct (confirmed via a Minuit2-only rebuild+restage+probe cycle: `etcdir=/rootsys/etc/` now correct, `opendir(etc/plugins)` succeeds, all 9 plugin macros' `ExecuteMacro` calls report success). Root-caused with temporary `printf`-based instrumentation (`::Error`/thrown-exception output is **not visible at all** in the `wasm/gates/rootweb/run.sh cell` capture pipeline — confirmed directly: a bare `fprintf(stderr, ...); fflush(stderr);` cell never appeared in `cell_stdout.txt`/`cell_diag.txt`, while `printf`-to-stdout always does; this explains why the killed agent's own `::Error`-based debug patches in `$R/core/base/src/TPluginManager.cxx` produced no visible signal despite being correctly placed and compiled in) added temporarily to `$R/core/base/src/TPluginManager.cxx` (synced to `~/.cache/rootwasm-p0/xbuild/src`, rebuilt via `cmake --build . --target Core`, restaged via `rootlight/run.sh stage`+`link` — a ~15-second cycle, not a full `Hist` rebuild) and to `TCppInterOpInterpreter::ExecuteMacro`/`ProcessLine` temporarily: every one of the 9 real plugin macros' top-level function ran (confirmed by an added `printf` inside a copy of `P010_Minuit2Minimizer.C`, which printed **twice** per call — `Cpp::Evaluate` reports `hadError=1` for a void-returning call and `TCppInterOpInterpreter::ProcessLine`'s existing fallback (`Cpp::Declare(line)`) re-executes the same statement, a pre-existing, harmless-here double-invocation quirk, not touched), but never reached its own post-`AddHandler` print — `TPluginManager::AddHandler` (`TPluginManager.cxx:568-579`) calls `gInterpreter->GetCurrentMacroName()` unconditionally whenever `TPH__IsReadingDirs()` is true (true for every macro-driven registration), and that virtual had no hand-written override, so `gen_fatal.py`'s generated override (`rkUnsupported` → `throw std::runtime_error`) fired mid-JIT, unwinding out of `AddHandler` **before `fHandlers->Add(h)` ever ran**, silently, once per macro, for all 9. Fixed with a two-line override matching ROOT's own base-class default exactly (`TInterpreter.h:259`: `virtual const char *GetCurrentMacroName() const {return nullptr;}`) — the same "adopt ROOT's own documented harmless default" move as fact 22's non-pure-virtual convention, not new tracking logic; declared in the header and named in `implemented.txt` (`wasm/rootlight/interpreter/TCppInterOpInterpreter.h`, `.cxx` — no `.cxx` body needed beyond the inline header override; `implemented.txt`). All temporary instrumentation (in `$R`'s `TPluginManager.cxx`, this adapter's `ExecuteMacro`/`ProcessLine`, and the test plugin macro file) was removed and the restored `TPluginManager.cxx` diffed byte-identical against a fresh copy before moving on. **Verified**: `gROOT->GetPluginManager()->FindHandler("ROOT::Math::Minimizer","Minuit2")` now returns non-null, `h->GetClass()` reports `ROOT::Minuit2::Minuit2Minimizer`, in a real browser cell.
    - **Fix 3: implemented and verified correct for its scoped shape; stopped, per this task's own instruction, at a materially bigger family one level deeper.** Implemented `CreateListOfBaseClasses` (`TCppInterOpInterpreter.cxx`, mirroring `TCling::CreateListOfBaseClasses`'s own `while (t.Next())` idiom) plus the narrow `BaseClassInfo_{Factory,Next,Delete,ClassInfo,FullName,Offset}` slice via genuine `Cpp::GetNumBases`/`Cpp::GetBaseClass`/`Cpp::GetBaseClassOffset` (`CppInterOp.h:500,506,514`) — real primitives, not fabricated numbers. `BaseClassInfo_Property`/`_TmpltName`/`_Name`/`_Tagnum` and the two-arg `BaseClassInfo_Factory`/`onlyDirect BaseClassInfo_Next` overloads are deliberately left to `gen_fatal.py`'s loud default: CppInterOp 1.9's whole header has no per-base access-specifier or virtual-base predicate at all, so those specific bits cannot be computed without guessing. A second, empirically-discovered primitive was also needed and implemented genuinely: `ClassInfo_FullName` (`TClass.cxx:3425`, `TClass::GetClass(ClassInfo_t*, Bool_t)`'s own name-lookup for a base's `TClass`, reached via `TBaseClass::GetClassPointer()`), via the same `Cpp::GetQualifiedName` convention as `BaseClassInfo_FullName`. **Verified correct** with a probe that first `#include`s `Minuit2/Minuit2Minimizer.h` (so the class is genuinely declared to clang, isolating this fix from the separate AutoParse gap below): `TClass::GetClass("ROOT::Minuit2::Minuit2Minimizer")` resolves a real `ClassInfo_t`, `CreateListOfBaseClasses` correctly finds **exactly 1 base** (`Cpp::GetNumBases` reports `1`, matching Minuit2Minimizer's real single public non-virtual inheritance from `ROOT::Math::Minimizer`), and `BaseClassInfo_FullName` correctly names it `"ROOT::Math::Minimizer"`. **Stopped** at the next boundary, found by continuing the probe one step further (calling `TBaseClass::GetDelta()`, which calls `Property()` first, which resolves the base's own `TClass` via `GetClassPointer()`, which runs `TClass::Init()` for `ROOT::Math::Minimizer` for the first time): `TClass::Init()` unconditionally calls `gCling->ClassInfo_ClassProperty(fClassInfo)` (`TClass.cxx:6281`) to compute `EClassProperty` bits — `kClassHasExplicitCtor`/`kClassHasImplicitCtor`/`kClassHasAssignOpr`/`kClassHasExplicitDtor`/`kClassHasImplicitDtor` (`TDictionary.h:135-148`) — and CppInterOp 1.9's public API (grepped the whole header for `Implicit`/`Explicit`/constructor/destructor predicates) has **no primitive that distinguishes a compiler-generated ("implicit") constructor/destructor/assignment-operator from a user-declared ("explicit") one** — `IsAggregate`/`IsAbstract`/`HasDefaultConstructor`/`IsConstructor`/`IsDestructor`/`IsExplicit` (on a *method*, meaning marked `explicit`, an unrelated sense of the word) exist, but nothing answers "is this specific special member compiler-synthesized". Fabricating these bits would be exactly the guessing the project rules forbid more strongly than an honest unimplemented stub. This is a **materially bigger, different family** — fully initializing an arbitrary base class's own `TClass`, not "walk one direct base list" — squarely the complexity this task's own instruction named as the stop condition. Not implemented; left loud (both `ClassInfo_ClassProperty` and `ClassInfo_Property` are absent from `implemented.txt`, so `gen_fatal.py` still generates their throwing overrides).
    - **A fourth, deeper, and probably more material wall, found while isolating Fix 3's own probe.** Without the explicit `#include` above (i.e. the real `fit.cxx` shape, which never names `ROOT::Minuit2::Minuit2Minimizer` in compilable C++ — the whole point of the plugin architecture is that user code never spells the concrete minimizer class), `TClass::GetClass("ROOT::Minuit2::Minuit2Minimizer")` still returns a valid, non-null `TClass*` (via the compiled-dictionary fast path, `TClassTable::GetDictNorm`, `TClass.cxx:3061`), but `cl->fClassInfo` stays `nullptr` and `GetListOfBases()` returns `nullptr` **silently** (ROOT's own documented "no ClassInfo available" degradation, not an error) — confirmed with temporary instrumentation in `$R`'s `TClass::GetListOfBases` (same printf-not-Error technique as Fix 2's diagnosis, same careful revert+resync+rebuild+diff-verify afterward): `fCanLoadClassInfo=1 fHasRootPcmInfo=0 fClassInfo=0` before `LoadClassInfo()`, still `fClassInfo=0` after it. `TClass::LoadClassInfo()` (`TClass.cxx:5932-5963`) calls `gInterpreter->AutoParse(GetName())` before `SetClassInfo` — and `AutoParse` (Family 0, already flagged "genuine" in fact 22) has **nothing to parse**: grepping the generated dictionary sources directly (`~/.cache/rootwasm-p0/xbuild/build/math/minuit2/G__Minuit2.cxx:2110-2112` **and** `~/.cache/rootwasm-p0/xbuild/build/hist/hist/G__Hist.cxx:16710-16712`, i.e. not a Minuit2-specific gap) shows `static const char* classesHeaders[] = { nullptr };` — **completely empty**, for every dictionary in this cross-build, an artifact of the `-writeEmptyRootPCM` minimal-dictionary generation mode this project already uses (fact-noted in this adapter's own `RegisterModule` comment). The module's overall `headers` array (a *different*, unused field — confirmed by grep: `Module::fHeaders` is written in `RegisterModule` but read nowhere in this file) does list `"Minuit2/Minuit2Minimizer.h"`, but nothing ever declares it to clang. So: a class can only get a genuine interpreter `ClassInfo_t` in this build if some earlier C++ code in the same session already named/`#include`d it explicitly — true for `TH1D`/`TF1` (the M4a gate's own `fit.cxx` literally `#include`s both) but categorically never true for a minimizer resolved purely by plugin string name. This is independent of, and predates, Fix 3's own base-class-walking scope; Fix 3 could not have solved it, and per this task's instructions on `CreateListOfBaseClasses` specifically, is reported rather than chased further this session.
    - **`bash wasm/gates/rootformula/run.sh loud`, exact verbatim result:** `rootformula M4a FAIL: TH1::Fit returned a result, but TClass reflection is not implemented -- a fit nothing computed is exactly the silent no-op this project forbids` — byte-for-byte the same message as fact 22 (`fit.out`: `fit.start`/`fit.returned` only; `fit.diag`: 0 bytes). **No plausible fit parameters were produced.** The gate script's own assertion cannot distinguish *why* `fit.diag` is empty, so the surfaced message is unchanged even though the internal cause has moved: previously (fact 22) the plugin-registration gap was hit first on this exact path; now that Fix 2 closes it, the path most likely reaches the wall from this fact's own paragraph above instead (`TClass::GetClass` degrading silently to a dictionary-only, interpreter-free `TClass` for the minimizer, which cannot satisfy `GetMethodWithPrototype`'s interpreter-based construction path) — consistent with `Factory.cxx:106-107`'s already-documented no-diagnostic `nullptr` return on `CreateMinimizer` failure. `bash wasm/gates/rootformula/run.sh parity` still passes (`p1: native CERN ROOT 6.40.04 == wasm ROOT in Chromium, 21 values`) — no regression. `wasm/gates/rootformula/run.sh` itself was **not edited**, per this task's explicit instruction.
    - **Build and tree state.** `bash wasm/rootlight/interpreter/build.sh` is clean: `gen_fatal: 231 loud-failure overrides, 59 implemented by hand` (up from fact 22's `243/49` end-state by the `ExecuteMacro`/`GetCurrentMacroName`/`ClassInfo_FullName`/`CreateListOfBaseClasses`/6 `BaseClassInfo_*` names added this session and last). Nothing committed. Files touched this session, beyond this `STATE.md` entry: `wasm/rootlight/interpreter/TCppInterOpInterpreter.{h,cxx}` and `implemented.txt` (the three fixes' genuine code), plus the pre-existing fact-22 staging diffs in `wasm/gates/rootlight/run.sh`, `wasm/gates/rootweb/{run.sh,node-probe.mjs,page/kernel-worker.js}`, and `wasm/gates/p0deps/xbuild/host-rootcling.patch` (all confirmed already-correct, untouched further). All temporary `printf`/`::Error` instrumentation added to `$R`'s `TPluginManager.cxx` and `TClass.cxx` and to a staged plugin-macro `.C` file during diagnosis was fully reverted and diff-verified byte-identical against pristine copies before the final build; `$R` has no VCS of its own, so this diff-against-a-fresh-copy check is the only integrity guarantee available, and was applied to every file this session edited outside the git-tracked repo.

24. **The Emscripten-dlopen-static-init hypothesis for fact 23's fourth wall is falsified by direct source reading plus a fresh empirical probe: conclusion (a), expected ROOT behavior, not a loader gap. Nothing implemented, nothing committed.**
    - **`TGenericClassInfo`'s constructor registers unconditionally; it is not deferred to first `TClass::GetClass`.** Every `TGenericClassInfo` constructor (`$R/core/meta/src/TGenericClassInfo.cxx:85-200`, all three overloads) ends by calling `Init(pragmabits)` (`:120`, `:159`, `:199`), and `Init` (`:250-262`) unconditionally calls `GetAction().Register(fClassName, fVersion, fInfo, fDictionary, pragmabits)` whenever `fAction` is non-null (true for every real dictionary; only a hand-rolled `TInitBehavior` override could suppress it, and none exists for Minuit2Minimizer). `TDefaultInitBehavior::Register` (`$R/core/base/inc/Rtypes.h:174-178`) forwards straight to `ROOT::AddClass(cname, id, info, dict, pragmabits)`, which (`$R/core/cont/src/TClassTable.cxx:915-929`) calls `TClassTable::Add(...)` directly once `TROOT::Initialized()` is true (queues to a delayed list only during ROOT's own early boot, flushed right after). This registration step is separate from and happens well before any `TClass` object is built: `TGenericClassInfo::GetClass()` (`:290-...`), which actually constructs the `TClass`, is only invoked later through the stored `fDictionary` function pointer, on demand. Conclusion: static-init alone genuinely is sufficient for `TClassTable::Add` to fire — the task's background hypothesis that this determines whether "static init not running" is even plausible is correctly framed, and the constructor is unconditional, not self-referential/lazy.
    - **Empirical test 1 (control, per this task's step 2): does `TClassTable`'s registration actually fire for these dlopen'd side modules in this exact build?** Wrote a throwaway cell (`bash wasm/gates/rootweb/run.sh cell <file>`, scratch file only, not committed, matching the established convention) calling the public API `TClassTable::GetDict(name)` (`$R/core/cont/inc/TClassTable.h:90`, a pure lookup into the registration table with no autoload/rootmap side effect, confirmed by reading `TClassTable::GetDict`'s body, `$R/core/cont/src/TClassTable.cxx:692-702` — it cannot return non-null unless `TClassTable::Add` already ran) for `"TObject"`, `"TH1D"`, and `"ROOT::Minuit2::Minuit2Minimizer"`, run immediately after the existing kernel/library-loading sequence (no added loading code, `Minuit2` is already in `run.sh`'s `LIBS` array). Verbatim result (`wasm/build/rootweb/cell_stdout.txt`): `dict.TObject=1`, `dict.TH1D=1`, `dict.Minuit2Minimizer=1`. **All three, including Minuit2Minimizer, are registered.** This directly falsifies "Emscripten `dlopen()` doesn't run `.init_array`/global ctors for SIDE_MODULEs" for this build: if it were true, all three (or at minimum the newly-added `libMinuit2.so`) would show `0`. Nothing is Minuit2-specific about the registration step; it is systemic and it works.
    - **Empirical test 2 (attempted, per this task's step 2's `TClass::GetClass(..., kFALSE)` comparison): informative but methodologically flawed as first written, corrected in place.** A cell calling `TClass::GetClass(name, kFALSE)` (i.e. `load=kFALSE`) for all three classes printed `cl=0` for all three, including `TObject` — not evidence of a registration failure but of the `load` parameter's own documented short-circuit: `TClass::GetClass` (`$R/core/meta/src/TClass.cxx`, the block starting at `:3059`) reaches `if (DictFuncPtr_t dict = TClassTable::GetDictNorm(name)) { if (!cl && !load) return nullptr; ... }` — `load=kFALSE` on a first-ever request returns `nullptr` unconditionally, *before* checking whether the dictionary is registered, so it cannot distinguish "not registered" from "registered but not asked to materialize." Recomputed with `load` defaulted to `kTRUE` (the call shape every real caller, including this project's own facts 22/23 probes, actually uses) exposed a third, unrelated, previously-undocumented wall instead (see below), so this half of the test was superseded by test 1 (which already answers the registration question unambiguously) and by fact 23's own instrumentation-based finding (next bullet), rather than pushed further.
    - **Why the interpreter path is still reached: ROOT's own two-tier laziness, not a fast-path failure — already established by fact 23, now cross-checked against the fast-path source directly.** `TClass::GetClass`'s compiled-dict branch (`TClass.cxx:3059-3067`, quoted in this task's own background) calls `(dict)()` and returns the resulting `TClass*` immediately on success — it never touches `fClassInfo` or calls into the interpreter at all. `fClassInfo` is left null and is only populated later by `TClass::LoadClassInfo()` (`TClass.cxx:5932-5963`), which is invoked on demand by whatever first needs real interpreter reflection (base-class walking via `GetListOfBases()`, `Property()`, `GetMethodWithPrototype()`, etc.) — and `LoadClassInfo()` is exactly what calls `gInterpreter->AutoParse(GetName())` then `SetClassInfo`. Fact 23 already verified this precise sequence for `ROOT::Minuit2::Minuit2Minimizer` with compiled-in `$R`-source instrumentation (not a JIT'd cell, so unaffected by test 2's incidental wall): `fClassInfo=0` immediately after the fast-path `TClass::GetClass` returns, still `0` right up to `LoadClassInfo()`'s own `AutoParse` call, which then fails to declare anything because this cross-build's dictionaries carry an empty `classesHeaders` table (`-writeEmptyRootPCM`, `~/.cache/rootwasm-p0/xbuild/build/math/minuit2/G__Minuit2.cxx:2110-2112`, confirmed not Minuit2-specific by the same empty array in `.../hist/hist/G__Hist.cxx:16710-16712`). Nothing in this session's fresh reading of `TGenericClassInfo`/`TClass::GetClass`/`PostLoadCheck` contradicts or narrows that finding; test 1 additionally confirms the *registration* step (the part fact 23 had not directly isolated) also behaves exactly as ROOT's source says it should.
    - **A fourth, distinct, previously-undocumented wall found incidentally while correcting test 2, reported and not chased (out of this task's scope).** Calling `TClass::GetClass(name)` (single-arg, default `load=kTRUE`) from a **freshly JIT'd interactive cell** — for `TObject` alone, before even reaching `TH1D`/Minuit2Minimizer — fails cell compilation itself with `Error in <TCppInterOpInterpreter>: TInterpreter::MethodInfo_GetMangledName is not implemented in wasm ROOT yet` (verbatim, `wasm/build/rootweb/cell_diag.txt`; reproduced twice, once with the three calls in one cell and once split into three separate top-level statements with no member-call chaining, ruling out `->GetClassInfo()` chaining as the trigger). `TClass::GetClass` has 4-5 overloads with defaulted arguments (`TClass.h:601-606,651,658`); this is evidently the incremental-module JIT needing to resolve which pre-compiled `libCore.so` symbol to dynamically link the call against, via a `MethodInfo`-family primitive this adapter does not implement (confirmed absent from `implemented.txt`; only mentioned in a comment at `TCppInterOpInterpreter.cxx:1192` as still-unimplemented). This is orthogonal to the registration/AutoParse chain above — it is a JIT/dynamic-linking gap specific to calling an already-compiled, multiply-overloaded static method directly from typed-in cell code, not something the compiled `TH1::Fit`→`Minuit2Minimizer` call chain itself would ever hit (that chain runs entirely inside already-compiled `.so`s, no per-call JIT symbol resolution needed). Not implemented or investigated further; flagged here only because it blocked a cleaner version of test 2 and because it is new information not in facts 1-23.
    - **Conclusion: (a).** The task's suspected Emscripten dlopen/static-initializer gap does not exist in this build: `TGenericClassInfo`'s constructor registers unconditionally at static-init time, and a fresh probe confirms `TClassTable::Add` fired for `TObject`, `TH1D`, **and** `ROOT::Minuit2::Minuit2Minimizer` alike. `TClass::GetClass`'s interpreter path is reached later only because ROOT's own design defers `ClassInfo`/ full interpreter reflection until something explicitly needs it (`LoadClassInfo`), which is legitimate, documented behavior, not a degraded fallback — and the actual reason `TH1::Fit`'s `Minuit2Minimizer` chain stalls there remains exactly fact 23's already-diagnosed, narrower cause (empty `classesHeaders` under `-writeEmptyRootPCM`), not this task's hypothesis. Per this task's "do not touch" instructions, no fix was attempted (there is nothing architectural to fix; fact 23's already-scoped `AutoParse`-input gap is a build/dictionary-generation-mode question, not a small clean adapter fix). `wasm/rootlight/interpreter/TCppInterOpInterpreter.{h,cxx}` were **not modified** this session. Nothing committed; the only file touched is this `STATE.md` entry. Probe cells were scratch files outside the repo (gitignored `wasm/build/rootweb/` build output only), not retained.

25. **Fact 23's fourth wall (empty `classesHeaders` under `-writeEmptyRootPCM`) fixed for real with a `GetDeclFileName()` fallback in `AutoParse`; `TH1::Fit` now stops loudly one step later, at `BaseClassInfo_Property` — the exact base-class-`TClass`-initialization family fact 23 already scoped as materially bigger and out of this task's reach. Not committed.**
    - **Ordering verified empirically before touching any code, per this task's own instruction.** A throwaway cell (`bash wasm/gates/rootweb/run.sh cell <file>`, scratch only, not retained) calling `TClass::GetClass("ROOT::Minuit2::Minuit2Minimizer")` (default `load=kTRUE`) then `->GetDeclFileName()`/`->GetClassInfo()` printed, verbatim: `cl=0x5400a50`, `declfilename=[Minuit2/Minuit2Minimizer.h]`, `fClassInfo=0`. This directly confirms the task's hypothesis: the real header string is already sitting on the compiled-dictionary-constructed `TClass` object well before anything calls `LoadClassInfo()`/`AutoParse()` on it, exactly as `TGenericClassInfo`'s constructor (`$R/core/meta/src/TGenericClassInfo.cxx:92,130,170`, `fDeclFileName(declFileName)` in the member-initializer list, before `Init(pragmabits)` runs in the constructor body) and `TClass::Init()` (`$R/core/meta/src/TClass.cxx:1359` sets `fDeclFileName` — first; `:1432` `TClass::AddClass(this)` registers it into `gROOT->GetListOfClasses()` — second; `:1486` the (here-failing) `CheckClassInfo` attempt — third, all three still inside the *same* `Init()` call, well before any later, separate `LoadClassInfo()` call) predict by direct source reading, not assumption.
    - **Why `TClass::GetClass(cls, kFALSE)` finds the already-registered `TClass` instead of returning `nullptr` or recursing.** Read `TClass::GetClass`'s body directly (`TClass.cxx:3000-3068`): the `load=kFALSE` short-circuit `if (!cl && !load) return nullptr;` (`:3062-3063`) only fires when `cl` — the result of `gROOT->GetListOfClasses()->FindObject(name)` (`:3013`/`:3025`) — is still null. Fact 24's test 2 hit that branch only because it queried a class *before* anything had ever constructed its `TClass` in that session. Here, `AutoParse(cls)` is called from inside `TClass::LoadClassInfo()`, which is a method called *on* an already-existing `TClass` object for `cls` — so `cl` is non-null (it is that very object, already added to the list at `Init():1432` long before `LoadClassInfo()` is ever invoked on it), the short-circuit is skipped, and the code falls through to `TClassTable::GetDictNorm(name)` → `(dict)()` → `TGenericClassInfo::GetClass()`, which (`TGenericClassInfo.cxx:310`, `if (!fClass && fAction)`) just returns the already-cached `fClass` pointer with **no re-entry into `Init()`/`CheckClassInfo`/`AutoParse`** — so there is no recursion risk, only a plain lookup of already-built data.
    - **The fix**, in `AutoParse(const char *cls)` (`wasm/rootlight/interpreter/TCppInterOpInterpreter.cxx:157-207`): when `fClassesHeadersMap` has no entry for `cls` (the `-writeEmptyRootPCM` case), fall back to `TClass::GetClass(cls, kFALSE)`; if non-null and `GetDeclFileName()` is non-empty, treat that one header exactly like a `classesHeaders`-sourced one — same dedup set (`fAutoParsedHeaders`), same `#include <header>` via `Cpp::Declare`, same error/exception convention on failure. This is genuine ROOT dictionary data reaching `AutoParse` through a different but equally real path (the compiled `TGenericClassInfo`'s own constructor arguments, baked in by `rootcling` from the real header, not fabricated or guessed), the same principle fact 19 already established for the `classesHeaders`-table case — not a workaround, and not the forbidden hand-written name-to-header translation table.
    - **Build: clean, no new unimplemented-name churn.** `bash wasm/rootlight/interpreter/build.sh` → `gen_fatal: 231 loud-failure overrides, 59 implemented by hand` — identical count to fact 23's end-state, because this fix only changed control flow inside an already-`implemented.txt`-listed `AutoParse`, adding no new `TInterpreter` overrides.
    - **`bash wasm/gates/rootformula/run.sh loud`, exact verbatim result:** `TH1::Fit stops loudly at: TInterpreter::BaseClassInfo_Property is not implemented` / gate reports `rootformula M4a PASS` (the gate's own assertion is "stops loudly, names the missing service", which this satisfies). `fit.diag` verbatim: `Info in <TCppInterOpInterpreter::CheckClassInfo>: S0 miss: TObjString` / `Error in <TCppInterOpInterpreter>: TInterpreter::BaseClassInfo_Property is not implemented in wasm ROOT yet` / `Could not load dynamic lib: /tmp/incr_module_3.wasm` / `[object WebAssembly.Exception]` / `Failed to execute via ::process:Failed to load incremental module`. This is the boundary moving *later and louder*, not sideways: the fix closed the silent-no-op wall (fact 23's `fit.diag`-empty case), and the very next thing `TH1::Fit`'s call chain needs — `EClassProperty` bits for `Minuit2Minimizer`'s base class `ROOT::Math::Minimizer`, reached via base-class-list walking during that base's own `TClass::Init()` — is exactly the family fact 23 already found and correctly stopped at (`ClassInfo_ClassProperty`/`BaseClassInfo_Property` needing a compiler-generated-vs-user-declared special-member predicate CppInterOp 1.9's public API does not expose; fabricating it would be guessing, forbidden more strongly than an honest unimplemented stub). **No plausible fitted parameters were produced** — `TH1::Fit` never reaches `Minimizer` construction on this path. Per this task's step 4, this new wall is reported rather than chased further.
    - **`bash wasm/gates/rootformula/run.sh parity`:** unchanged, `p1: native CERN ROOT 6.40.04 == wasm ROOT in Chromium, 21 values` — no regression.
    - Nothing committed. Files touched this session, beyond this `STATE.md` entry: `wasm/rootlight/interpreter/TCppInterOpInterpreter.cxx` (the `AutoParse` fallback only — no header/`implemented.txt` changes needed, the override already existed). The probe cell was a scratch file outside the repo, not retained.

26. **`BaseClassInfo_Property` checked as its own, narrower question, per this task's charge — confirmed genuinely narrower than fact 23's implicit-special-member family, but equally unimplementable without guessing. Nothing implemented, nothing committed.**
    - **Every real consumer of `TBaseClass::Property()` found and traced.** Grepped all of `$R/core/meta/src` for `->Property()` calls on objects with `TBaseClass` static type (not `TClass`/`TDataMember`/`TFunction`, which share the `Property()` name but call different `gCling` entry points). Exactly four call sites exist, and all four are real, reachable code, not dead: `TBaseClass.cxx:80` (`TBaseClass::GetDelta()`, tests `kIsVirtualBase`), `TClass.cxx:2792` (`TClass::GetBaseClassOffsetRecurse`, tests `kIsVirtualBase`), and `TViewPubDataMembers.cxx:39` / `TViewPubFunctions.cxx:39` (both `AddBasesClasses`, tests `kIsPublic`). `EProperty`'s bit layout (`$R/core/meta/inc/TDictionary.h:75-89`) confirms `kIsPublic`/`kIsProtected`/`kIsPrivate` (`0x200`/`0x400`/`0x800`) and `kIsVirtualBase` (`0x200000`) are the relevant nominal bits for a base-class relationship; no other bit in the enum is base-relevant. So: **exactly two facts are ever asked of a `BaseClassInfo_Property` result — "is this base virtual?" and "is this base's access public?"** — never the implicit-ctor/assignment bits (`kIsImplicit...`) that make up fact 23's `ClassInfo_Property`/`ClassInfo_ClassProperty` family. Fact 25's characterization of the two as "exactly the family fact 23 already found" was imprecise: **this task confirms `BaseClassInfo_Property` is a materially narrower, separate two-bit question**, not the multi-bit implicit-special-member predicate.
    - **Confirmed on this task's specific call chain**, not just in the abstract: `Minuit2Minimizer : public ROOT::Math::Minimizer` (`$R/math/minuit2/inc/Minuit2/Minuit2Minimizer.h:58`) is plain, single, non-virtual, public inheritance — no `virtual` keyword, one base, explicit `public`. `TClass::GetBaseClassOffsetRecurse` (reached via `GetBaseClassOffset`, needed for `this`-pointer adjustment whenever a `Minimizer` virtual method is called through a `Minuit2Minimizer` instance) and `TBaseClass::GetDelta()` are the plausible reachable callers on the `TH1::Fit` path; both need only `kIsVirtualBase`.
    - **CppInterOp 1.9's public API cannot truthfully answer either bit — verified by reading its own implementation, not just its header.** `CppInterOp.h` has no `IsVirtualBase`/`IsPublicBase`/`GetBaseAccessSpecifier`-shaped function anywhere (only method- and variable-level `IsPublicMethod`/`IsProtectedMethod`/`IsPrivateMethod`/`IsPublicVariable`/etc. — nothing for a base-class edge). Read `GetBaseClass`'s actual implementation (`$R/interpreter/CppInterOp/lib/CppInterOp/CppInterOp.cpp:834-843`): it walks `CXXRD->bases_begin() + ibase` — which **is** the real `CXXBaseSpecifier`, carrying genuine `isVirtual()`/`getAccessSpecifier()` bits — but extracts only `->getType()` and returns the resulting scope `Decl*`. The virtual/access bits are read by CppInterOp's own code and then discarded before the public API returns anything; no sibling function re-exposes them. `GetBaseClassOffset` (`:915` onward) also touches `CXXBasePathElement::Base->isVirtual()` internally (in the `static ComputeBaseOffset`/`isDerivedFrom(..., Paths)` machinery) but again only to fold it into a numeric offset, never surfacing a virtual/non-virtual predicate a caller could read back honestly and generally. This is the same public-API boundary rule this task's own instructions draw: CppInterOp's headers and implementation were read to characterize the boundary, not to reach through it into raw clang internals for the actual implementation.
    - **Stop condition applies, deliberately.** Hard-coding "always non-virtual, always public" would answer *this one* class correctly (confirmed by eye above) but would silently mis-answer any class with a protected/private or virtual base — `TInterpreter::BaseClassInfo_Property` has no way to know which case it is in without the missing primitive, so a fixed default is exactly the "plausible but wrong" guess the stop condition forbids, not a narrow-but-honest special case. **Outcome: stop condition confirmed, for a genuinely different (narrower, two-bit, non-implicit-ctor) reason than fact 23/25 stated — `BaseClassInfo_Property` remains an honest loud stub.**
    - Nothing implemented, nothing rebuilt, nothing committed. Gates not re-run (no code change to validate); fact 25's `rootformula run.sh loud`/`parity` results stand unchanged. Files touched this session: none besides this `STATE.md` entry (investigation only — `Read`/`Bash` grep and `sed -n` reads of `$R` and the CppInterOp header/source).

27. **`/codex:adversarial-review` ran against the M4b working-tree diff (2026-09-17), verdict `needs-attention`, 2 high + 3 medium findings. Four fixed for real, one falsified by direct measurement and left unchanged.**
    - **Fixed: Minuit2 was never built by the actual producer gate.** `wasm/gates/p0deps/xbuild/run.sh` built only the `Hist` CMake target; Minuit2 is not in Hist's dependency closure, so a clean cache's `rootlight/run.sh stage` (which already listed `Minuit2` in `LIBS`) would fail to find `libMinuit2.so`. The existing local cache masked this because Minuit2 had only ever been built by a one-off manual `cmake --build . --target Minuit2` during an earlier Agy session. Fixed: the `graph` step now walks and FORBIDDEN-checks both `hist/hist/CMakeFiles/Hist.dir/all` and `math/minuit2/CMakeFiles/Minuit2.dir/all` (both closures pass, `FORBIDDEN: none`, confirmed by rerunning `graph` fresh — 27-target Minuit2 closure, no interpreter/cling/llvm/clang node in either), the `build`/`hist`/`all` step now builds `Hist Minuit2` together (`cmake --build . --target Hist Minuit2`, CMake 3.15+'s repeated/multi-target `--target` syntax; verified: exit=0, 207s), and the `inspect` step's `libs` tuple now includes `Minuit2` with `TMinuit2TraceObject::Class()` as its representative dictionary symbol (`Minuit2Minimizer` itself carries no compiled `Class()` — confirmed absent via `llvm-nm`; it is genuinely constructed through interpreter reflection via `TPluginManager`, matching this whole milestone's premise, not a compiled dictionary). Full chain reverified after the fix: `xbuild inspect` → `xbuild Hist PASS` (7 libraries now), `rootlight stage` + `link` → all 7 side modules link including `libMinuit2.so`, `rootformula run.sh` → unchanged 21-value parity PASS and the same `BaseClassInfo_Property` stop (fact 26) — no regression.
    - **Fixed: non-static member calls through `CallFunc` had no documented boundary.** `MakeRootCallable` (`TCppInterOpInterpreter.cxx`) emits `Qualified::Name(args...)` as a bare expression, which is a hard C++ compile error for a genuine non-static method — so `Cpp::Declare` already failed and `fResolved` stayed false; nothing was dereferencing a null object today (the review's "high" severity overstated the live risk). But that safety was accidental (a side effect of C++ grammar), not a stated contract, and Family F's own doc comment implied general method-call coverage. Both `CallFunc_SetFuncProto(..., bool objectIsConst, ...)` and `CallFunc_SetFunc(CallFunc_t*, MethodInfo_t*)` now check `Cpp::IsMethod(resolved) && !Cpp::IsStaticMethod(resolved)` (excluding constructors) and call `rkUnsupported("CallFunc on a non-static member function")` before ever reaching `MakeRootCallable` — an explicit, named boundary instead of an incidental compile failure. Real instance-method dispatch (threading `this` through `JitCall::Invoke`) remains unimplemented; nothing on the `TH1::Fit` path needs it.
    - **Fixed: `objectIsConst` was accepted but never used in overload selection**, in both `CallFunc_SetFuncProto` and the previously-unnoticed `GetFunctionWithPrototype` (same bug, same root cause, one more call site than the review named). `ResolveFunctionWithPrototype` now takes `objectIsConst` and filters candidates to `Cpp::IsConstMethod(f)` before `BestOverloadFunctionMatch` when true — cheap and correct even though the only live caller today resolves a constructor (no const/non-const split), so it was unreached, not silently wrong, in the current call graph.
    - **Fixed: `AutoParse` poisoned its own retry on a failed `#include`.** `fAutoParsedHeaders.insert(header)` ran before checking whether `Cpp::Declare` of the `#include` succeeded; a failure still marked the header parsed, so a later retry (e.g. after an include-path fix) would silently return 0 forever. Now insertion happens only after `Cpp::Declare` returns success.
    - **Not fixed — directly measured and falsified.** The review's claim that `ExecuteMacro` double-invokes a plugin macro's function (attributed to `ProcessLine`'s `Cpp::Evaluate`-then-`Cpp::Declare` fallback re-executing a void call) does **not** reproduce under the current code. Two independent tests: (1) declare a counter and a void function via `gInterpreter->Declare`, call it once via `gInterpreter->ProcessLine("fn();", &err)` (the exact mechanism `ExecuteMacro`'s tail delegates to) — counter reads 1, not 2. (2) The real, unmodified `TInterpreter::ExecuteMacro` path end to end: a throwaway `printf("HIT\n")`-instrumented `.C` file staged into the actual mounted `/rootsys/etc/` (not a synthetic simulation), invoked via `gInterpreter->ExecuteMacro("/rootsys/etc/rk_probe.C", &err)` from a real browser cell — exactly one `HIT` printed. This directly contradicts this file's own fact-in-progress note under "Fix 2" (the `GetCurrentMacroName` investigation), which recorded "printed **twice** per call" and attributed it to this same Evaluate/Declare mechanism. That attribution appears to have been a plausible-sounding but unverified guess made in the moment, not independently re-tested at the time (or the twice-observed behavior there had a different cause — e.g. `TPluginManager`'s own two-phase handler scan calling the macro's function via a separate path — that was never isolated). Nothing in `ExecuteMacro`/`ProcessLine` was changed for this finding: measured, not guessed, and the measurement says the code is correct as written. Throwaway probes discarded, not committed; staged `/rootsys/etc/rk_probe.C` removed after the test.
    - Working tree remains uncommitted throughout, per standing instruction: no commit by any agent this session. A separate, external reviewer will make the commit decision.

## Gate-order correction

- Old assumption: G1 (a MathCore library build) must precede G2.
- Falsifying evidence: G2 ran without a ROOT library, while G1's package route enters Core → CLING.
- Second correction: G2's proven capability (GenVector/four-vectors) is not needed by any current or planned kata (fact 9). It validated the toolchain, not the product's ROOT surface.
- Third correction: G8 ("one kata fully client-side") requires compiling **the student's own C++** in the browser, which needs a C++ toolchain in the browser — a requirement independent of ROOT and unproven by any gate so far, including for the cheap header-only G2 slice.
- Replacement order: `G0 → G2 → G4 (blocker, documented) → G7 (PASS, compiler-in-browser) → TH1D build-system decision`.
- Fourth correction: G7 passed. A C++ compiler running client-side is no longer an open risk; it is confirmed (fact 11). The open risk moves entirely to `TH1D`/`TF1` — the curriculum's actual demand — which G7 did not touch.
- Fifth correction: the interpreter probe closes the cheap `TH1D` path without Core. The first-cell symbol failure under the pinned release means the next objective should scope a target-side Core cross-build, not another interpreter variation.
- Replacement order: `G0 → G2 → G4 (partial, documented) → G7 (PASS, compiler-in-browser) → Core cross-build scope → smallest validated ROOT capability`.
- Unresolved risk: GenVector does not establish a path for `TH1D`, files, or RDataFrame; ROOT Light is not yet viable as a useful curriculum subset on this evidence alone.

## Rejected approaches

- A hand-maintained `RConfigure.h`, ROOT stubs, or ROOT-like replacements.
- A full ROOT/MathCore/Core/Cling build to prove a header-only G2 claim.
- Treating browser rendering or JSROOT as ROOT computation.
- Stubbing `TH1D::Class()`/`Streamer()` to satisfy the linker: it would link, but is forking `TH1`, which the mission forbids (fact 8).
- Arguing the TH1D blocker from the CMake target graph alone: `add_dependencies` is build-order, not a link requirement (fact 8's correction).
- AOT clang+lld compiled to WebAssembly via any WASI-lineage prebuilt (`browsercc`, and by inheritance `binji/wasm-clang`, `wapm-packages/clang`): falsified for this project's needs by a shared, currently-open upstream `libc++abi` exception-handling gap (fact 11, H2).
- Treating the prebuilt xeus-cpp-lite interpreter as a cheap `TH1D` escape hatch: falsified by the first-cell `TVersionCheck` symbol wall; the interpreter still needs target-side Core.
- Building LLVM/Clang/LLD from source to fix the H2 gap: explicitly out of scope for a gate; not attempted.
- Resuming the pre-existing manual `gcore/` source manifest: retired diagnostic work, excluded from the P0 session commit. Use ROOT's own CMake targets.
- Treating E1's constant autoregistration interposition or an unverified `InitInterpreter` early return as a production port: configuration, dictionaries and cleanup require preserved ROOT behavior.

## Canonical reproduction

```bash
bash wasm/toolchain/install-emsdk.sh
bash wasm/gates/g7/run.sh # G7 PASS
bash wasm/gates/g4/interpreter-probe/probe.sh # BLOCKED-AT-SYMBOL (evidence)
```

## Historical G7 verified commit

`9fa5f547` — G7 implementation and close-out state, independently reproduced from
clean twice by Codex Sol High on 2026-09-13. The review also corrected the browser
interpreter arguments with `-fwasm-exceptions`; ordinary G2 execution did not expose
the omission, while a direct throw/catch probe did.

## Reviewer verdict

**M1 through M6a review: PASS**, independently reviewed 2026-09-16. The earlier
non-pure `TInterpreter` qualification is closed by 284 generated loud failures
plus an overload-coverage check. The four browser gates pass together; M6a
reproduces 11 native-identical, validator-passing exercises and two exact
`SetClassInfo` blockers. See [M1–M6a review](gates/CODEX-REVIEW-M3-M4.md); the
earlier [M1/M2 review](gates/rootlight/CODEX-REVIEW-M2.md) is historical.
